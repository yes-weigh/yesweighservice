rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny-all until app logic is implemented.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
